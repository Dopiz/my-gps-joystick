# Plan v2 — 產品迭代（懸浮選單 + 主頁/地圖重整 + GPX 管理）

依相依順序分三批執行。分支：feat/overlay-extensions。

## 判斷記錄（模糊項）
- **起始座標**：移除主頁手動經緯度「起始座標」卡片。開始模擬時以**目前真實位置**為起點；座標設定改由地圖點選／貼座標／最愛完成。加「回到真實定位」按鈕（停止 mock、放行真實 GPS）。

## Batch 1 — 核心 app 模型（MainActivity + MapActivity + 播放 + 速度 + 最愛）
- [ ] 權限按鈕「Mock app」→「Mock」。
- [ ] 移除主頁「起始座標」lat/lng 卡片。
- [ ] 新增「回到真實定位」按鈕（停止注入、還原真實定位）。
- [ ] MapActivity 加返回鍵（toolbar up / 系統返回）。
- [ ] 播放模式改三種：**一次 / 巡迴 / 往返**；只保留單一「開始」鍵（移除 播放/續播 兩鍵；暫停另處理）。
- [ ] 「播放速度」→「**移動速度**」；先三個預設（走/跑/車）再給 custom bar；與懸浮速度共用同一 SpeedModel。
- [ ] 地圖點任一處：**先把座標帶入輸入欄位**，使用者點「前往」才真的移動（取消即時瞬移）。
- [ ] 最愛：儲存時使用者可**自行命名**。

## Batch 2 — GPX 管理頁（匯入/匯出/保存/選用）
- [ ] 新增 GPX 管理頁：匯入 .gpx（複製進 app 儲存保存）、列出已存 GPX、匯出/分享、刪除。
- [ ] 匯入後保存，之後可選不同已存 GPX 進行播放。
- [ ] MapActivity 的 GPX 來源改走「已存 GPX」選單（沿用解析/畫線/播放）。

## Batch 3 — 懸浮地圖子選單 + icon 重設計
- [ ] 懸浮「地圖」child 改成**可展開子選單**（如速度那樣）：
  - 子項 1：開啟 app 地圖頁。
  - 子項 2：GPX **開始/暫停** 切換；無載入中的 GPX 則 **disabled**。顏色狀態：未載入＝原本黑白；載入進行中＝**藍色**；暫停中＝**黃色**。
- [ ] 重新設計懸浮 icon（走/跑/車/地圖/鎖等），圖案**置中**、更精緻、風格一致。
- 依賴 Batch 1 的播放模型與 Batch 2 的已存 GPX 狀態。
